# Quickshell Runtime Scaffold

This directory is reserved for Nix-side Quickshell integration files.

Planned responsibilities:

- generate normalized theme JSON for runtime
- expose active theme identity and asset paths
- keep runtime contract stable across themes

Checkpoint specs:

- checkpoint-01-lotm-ritual-bar.md
