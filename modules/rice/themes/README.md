# Themes

Each rice theme should live in its own directory.

Guidelines:

- Keep theme-specific options and mappings local.
- Do not import one theme from another.
- Prefer data generation over ad hoc string config.
